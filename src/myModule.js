const message = "my message is good morning";
const name = "abdelrazik";
const loaction = "cairo";

const getGreeting = (name) => {
  return `hello and welcome mr ${name}`;
};
export { message, name, loaction as default, getGreeting };
